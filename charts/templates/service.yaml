apiVersion: v1
kind: Service
metadata:
  name: {{ include "msg-signer.fullname" . }}
  labels:
    {{- include "msg-signer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "msg-signer.selectorLabels" . | nindent 4 }}
